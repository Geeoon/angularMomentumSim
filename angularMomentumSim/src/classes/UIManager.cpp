#include "UIManager.h"

UIManager::UIManager(sf::Font font1) {
	/*

	
	title.setFont(font);
	title.setString("Visualization of Angular Momentum by Geeoon Chung");
	title.setCharacterSize(24);
	title.setFillColor(sf::Color(0, 255, 65));
	*/
}